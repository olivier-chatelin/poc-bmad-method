{
  "name": "poc_bmad",
  "version": "1.0.0",
  "description": "Chatbot MCP Lab - MCP experimentation tool for developers",
  "private": true,
  "scripts": {
    "dev": "concurrently \"pnpm --filter frontend dev\" \"pnpm --filter backend dev\"",
    "dev:frontend": "pnpm --filter frontend dev",
    "dev:backend": "pnpm --filter backend dev",
    "build": "pnpm --filter frontend build && pnpm --filter backend build",
    "test": "pnpm -r test"
  },
  "keywords": ["mcp", "chatbot", "llm", "developer-tools"],
  "author": "Olivier",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
